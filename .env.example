# 로컬 개발 환경변수 예시. 복사해서 .env 로 쓰고 값 변경. (.env 는 git 제외)
#   docker compose 는 .env 를 자동으로 읽는다.
#   백엔드 앱(./mvnw spring-boot:run)에는 셸에서 export 하거나 IDE 실행 구성에 넣는다.

# 로컬 PostgreSQL 비밀번호 (compose 기본값과 맞춘다)
DB_PASSWORD=ilis
